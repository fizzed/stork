# example of setting system properties for play in both in dev and production
# via a config file and the help of the co.fizzed.stork.bootstrap.PlayBootstrap
# class that the stork play sbt plugin uses by default in its launcher config

http.port=9001